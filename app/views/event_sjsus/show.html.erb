<p id="notice"><%= notice %></p>
<!--
<p>
  <strong>Title:</strong>
  <%= @event_sjsu.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @event_sjsu.body %>
</p>
-->
<h2><%= @event_sjsu.title %></h2>

<p><%= @event_sjsu.body %></p>

<%= link_to 'Edit', edit_event_sjsu_path(@event_sjsu) %> |
<%= link_to 'Back', event_sjsus_path %>

<h3>Comments</h3>

<%= render @event_sjsu.comments %>

<h4>New Comment</h4>

<%= form_for [@event_sjsu, @event_sjsu.comments.build] do |f| %>
	<div class='field'>
		<%= f.label :author %><br>
		<%= f.text_field :author %>
	</div>
	<div class='field'>
		<%= f.label :body %><br>
		<%= f.text_area :body %>
	</div>
	<div class='actions'>
		<%= f.submit %>
	</div>
<% end %>
