<%= form_for(@event_sjsu) do |f| %>
  <% if @event_sjsu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_sjsu.errors.count, "error") %> prohibited this event_sjsu from being saved:</h2>

      <ul>
      <% @event_sjsu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
    <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
